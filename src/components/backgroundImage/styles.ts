import styled from 'styled-components/native';
// import {WINDOW_HEIGHT} from '../../global/theme';

export const Container = styled.SafeAreaView`
  flex: 1;
  position: relative;
`;

export const ImageBG = styled.Image`
  flex: 1;
  position: absolute;
  width: 100%;
`;
